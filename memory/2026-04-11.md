# Journal Entry - 2026-04-11

## Mission Control Progress
Built extensive retro gaming "Second Brain" web app over the past 24 hours:
- Next.js app at `workspace/second-brain`
- Connected to Plex API for media library
- Full game inventory (26,980 titles across 14 platforms) from Google Sheets
- Real-time PriceCharting market data fetching (background cron at midnight)
- Buy/Sell/Vibe scores, daily price history, trend analysis
- Favorites/Regrets per-person system
- Console info modals with daily rotating trivia
- Shared PriceDetailModal and PlatformButton components

## Feature Backlog (Greenlit by user)

### BUSINESS
- [x] Flip Score Dashboard (Sell Score column exists, needs dedicated tab)
- [ ] eBay Sold Listings Integration - **SKIP** (needs API key from user)
- [ ] Lot Purchase Calculator
- [ ] Acquisition Tracker (purchase log with date/location/cost)
- [ ] Sales Log (track sold games with realized P&L)
- [ ] Target Price Alerts (watchlist with "buy below" thresholds)
- [ ] Market Timing Indicators (30-day moving avg vs current price)

### VANITY
- [ ] Collection Completion Goals (per-console progress bars)
- [ ] Rarity Score (common/uncommon/rare/ultra-rare badges)
- [ ] Personal Review/Notes (1-5 stars + text per game)
- [ ] Collection Showcase Mode (visual cover art gallery)
- [ ] Nostalgia Score (sentimental value metric)
- [ ] Completed Games Tracker (Beaten/100%/Never Played status)

### DEFERRED (needs user action)
- eBay Sold Listings API - user must provide API key

## Session Continuation - 2026-04-11 afternoon

### Features Built This Session
- `data/app.config.json` — centralized config file (app name, tagline, currency, theme, plex url/token, fetch schedule, auth toggle+password hash, standalone mode flag)
- `/api/config` — REST API GET/POST/PUT for reading/writing config + SHA-256 password hashing
- `/settings` page — full settings UI with sections for Identity, Localization, Deployment, Plex, Price Data, and Authentication
- `AppConfig.tsx` context provider — app-wide config reads from the API
- `StandaloneNav.tsx` — horizontal top navbar for standalone/shareable RetroVault mode; conditionally replaces MC sidebar based on standaloneMode flag in config
- `CriticProfileModal.tsx` — extracted into standalone component, shows mentions from tags.json
- `data/themes.ts` — 8 color palettes + 5 style themes defined (Matrix Green, Cobalt Blue, Synthwave, Amber, Arcade Magenta, Retro Cyan, Crimson, Golden Age + Terminal, CRT Scanline, Arcade Cabinet, Cartridge, Dark Galaxy)
- `ThemeProvider.tsx` — CSS variable-based dynamic theming, persists to localStorage
- `data/tags.json` — new data file for game/platform tags and critic @mentions
- `/api/tags` — REST API for add/remove tags, add/delete mentions
- `CriticProfileModal.tsx` — updated to show incoming @mentions from tags.json

### In Progress at Session Cut
- `TagsPanel.tsx` — tags + @mentions UI component for embedding in game/console modals
  - Write was blocked by memory flush mode; need to complete
- Need to: wire TagsPanel into PriceDetailModal and ConsoleModal
- Need to: add tag/mention search to the Vault search bar
- Need to: wire ThemeProvider into layout.tsx
- Need to: add Theme Picker section to /settings page
- Need to: update layout.tsx to conditionally show StandaloneNav vs MC sidebar

### Queue After Tags/Search/Themes
- Tag-based search bar in Vault
- Theme picker in Settings page
- Wire StandaloneNav + ThemeProvider into layout.tsx

### Current State
- Build is green (all routes compiling OK as of last exec output)
- Tags API is live but TagsPanel component hasn't been written yet

## Feature Backlog (greenlit, not yet built)
- Global search bar in nav (across games, pages, grails, watchlist, events)
- Dashboard overhaul (real command center replacing placeholder content) ← IN PROGRESS
- Mobile PWA (manifest.json + service worker for home screen install) ← IN PROGRESS
- Price Alert Notifications (watchlist trigger engine) ← IN PROGRESS
- Keyboard shortcuts (/, G+V, G+F etc.)
- Collection Value History (daily snapshots + graph)
- Duplicate Detector (scan vault for accidental dupes)
- Flip Opportunity weekly digest (Discord/email)
- Per-game rich text notes/journal
- Collection Randomizer ("what should I play tonight?")
- CSV/CLZ import from other tools
- Public Collection URL (read-only shareable link)
- QR Code generator for games/collection

## Backlog Addition (2026-04-12)
- **SQLite migration** — replace JSON flat-file storage with SQLite + Prisma ORM
  - Motivation: better performance for large collections (10k+ items), concurrent write safety, faster queries
  - Scope: ~2-3 days. All API routes need updating. Data is a single portable `.sqlite` file.
  - Priority: do before any SaaS / multi-user work
  - Decision: launch with JSON for now (frame as a feature: transparent, portable, no setup)
